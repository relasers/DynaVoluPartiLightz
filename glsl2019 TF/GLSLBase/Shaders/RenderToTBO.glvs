#version 450

in vec4 a_Position;
in vec2 a_UV;
in vec4 a_Color;

out vec2 v_UV;
out vec4 v_Color;

void main()
{
	vec4 newPos = a_Position;
	
	newPos.x += 0.05f;
	v_UV = a_UV + vec2(0.1f);
	v_Color = a_Color + vec4(0.2f);

	gl_Position = newPos;
}
